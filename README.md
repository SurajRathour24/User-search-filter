# User Search Filter (HTML, CSS, JavaScript)

A simple user search interface built with pure HTML, CSS and JavaScript.  
It allows you to filter user cards in real time based on the search input.  
If no matching user is found, a fallback “No user found” / dummy card is displayed.

---

## 🔍 Features

- Live search filter for user cards.
- Case-insensitive name matching.
- Shows a **dummy card** when:
  - No user matches the search term.
  - Helpful message like “No user found with this name” is displayed.
- Clean and responsive UI using pure CSS.
- No external frameworks – **vanilla JavaScript** only.

---

## 🛠️ Tech Stack

- **HTML5** – structure and layout
- **CSS3** – styling and responsiveness
- **JavaScript (ES6+)** – search logic and DOM manipulation

---

## 📂 Project Structure

```bash
.
├── index.html      # Main page with search bar and user cards
├── style.css       # All styles for layout and design
└── script.js       # Search filter functionality and dummy card logic
